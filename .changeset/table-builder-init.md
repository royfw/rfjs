---
"@rfjs/table-builder": minor
---

New package: config-driven read-only data table engine over @rfjs/data-schema — `TableConfig`/`TableColumnConfig` types + zod schema, and `deriveTableConfig` to compile a `DataResourceMeta` into an editable table config (columns mapped from fields, pageSize defaults to 10).
