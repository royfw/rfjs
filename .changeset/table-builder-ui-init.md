---
"@rfjs/table-builder-ui": minor
---

New package: shared config-driven data table (React) over `@rfjs/table-builder` — `TableSource`/`TableLabels` types with English `DEFAULT_LABELS`, and `useConfigTable(config, source)` client-mode hook (`sortRows` + slice pagination, `toggleSort`/`setPageSize` reset to the first page). The remote-source path is a later task.
