# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies
node_modules
.pnp
.pnp.js

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel

# Build Outputs
.next/
out/
build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

.pnpm-store

# Generated by `changeset status --output` in CI (publish/version workflows) — transient, not source
changeset-status.json
# git worktrees (superpowers / parallel sessions)
.worktrees/

# brainstorming visual-companion mockups (transient, per-session)
.superpowers/

# Per-agent skill symlinks — regenerable from .agents/ + skills-lock.json
# via `npx skills experimental_sync`. The canonical skill source lives in
# .agents/skills/ (committed); agent-specific dirs are generated.
.claude/skills/
